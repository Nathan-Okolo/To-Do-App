<%- include("header") -%>
    <div class="box" id="heading">
      <h3 style="color: blueviolet"><h1 style="color: blueviolet">TODAY</h1> <%= listTitle %>!</h3>
    </div>

    <div></div>

    <div class="box">
      <% newListItems.map(items => { %>

        <form action="/delete" method="post">
          <div class="item">
            <input type="checkbox" value="<%= items._id  %>" onChange="this.form.submit()" name="checkedItem"/>
            <p><%= items.name %></p>
          </div>
        </form>

    <% }); %>
      <form action="/" method="post">
        <input type="text" placeholder="New Item" name="Item" autocomplete="off" />
        <button type="submit" name="list" value=<%= listTitle %>  >+</button>
      </form>
    </div>

    <%- include("footer") -%>
